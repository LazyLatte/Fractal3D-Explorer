#ifdef MANDELBULB
#include <mandelbulb.glsl>
#endif

#ifdef MANDELBOX
#include <mandelbox.glsl>
#endif

#ifdef MENGER
#include <menger.glsl>
#endif

#ifdef SIERPINSKI
#include <sierpinski.glsl>
#endif

#ifdef JULIA4D
#include <julia4D.glsl>
#endif

#ifdef UNKOWN
#include <unkown.glsl>
#endif